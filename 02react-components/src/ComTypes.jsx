function Types(){
    return(
        <>
        <p><b>1.Class Components: </b>These are ES6 classes that extend from React.Component and must have a render() method. They can hold and manage their own state and lifecycle methods.</p>
        <p>
            <b>2.Functional Components: </b>These are simpler than class components and are written as JavaScript functions. They receive props as their argument and return React elements.
        </p>
        </>
    )
}

export default Types;